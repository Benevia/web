/* ============================================
   BENEVIA GRUPO CONSULTOR — STYLESHEET
   Mobile-first, responsive, accesible
   ============================================ */

/* --------------------------------------------
   VARIABLES DE IDENTIDAD (extraídas de guía)
   -------------------------------------------- */
:root {
  /* Colores institucionales — Pantone exactos de guía */
  --benevia-blue: #0070B8;        /* Pantone 300 C — RGB(0,112,184) */
  --benevia-orange: #D26E00;      /* Pantone 166 C — RGB(210,110,0) */
  --benevia-green: #738F24;       /* Pantone 370 C — RGB(115,143,36) */
  --benevia-dark: #414141;        /* Pantone Black 7 C — RGB(65,65,65) */

  /* Colores subordinados */
  --benevia-blue-dark: #00517D;   /* Pantone 302 C */
  --benevia-cyan: #00A4E8;        /* Pantone Process Cyan C */
  --benevia-orange-red: #CC5200;  /* Pantone 179 C */
  --benevia-gold: #DE9800;        /* Pantone 144 C */
  --benevia-green-dark: #577721;  /* Pantone 364 C */
  --benevia-green-light: #95B82F; /* Pantone 368 C */
  --benevia-gray: #515054;        /* Pantone Violet C */
  --benevia-purple: #534090;      /* Pantone 2665 C */

  /* Neutros */
  --white: #FFFFFF;
  --cream: #F5F5F0;
  --light-gray: #E8E8E5;
  --mid-gray: #9A9A98;
  --text-primary: #333333;
  --text-secondary: #555555;

  /* Tipografía — SUPOSICIÓN DOCUMENTADA:
     Gotham requiere licencia comercial (Hoefler & Co.).
     Se usa Montserrat (títulos, similar geométrica) e Inter
     (cuerpo, similar a Helvetica pero optimizada web). */
  --font-titles: 'Montserrat', 'Helvetica Neue', Helvetica, Arial, sans-serif;
  --font-body: 'Inter', 'Helvetica Neue', Helvetica, Arial, sans-serif;

  /* Jerarquía de tamaños (mobile-first base 16px) */
  --text-xs: 0.75rem;    /* 12px */
  --text-sm: 0.875rem;   /* 14px */
  --text-base: 1rem;     /* 16px */
  --text-lg: 1.125rem;   /* 18px */
  --text-xl: 1.25rem;    /* 20px */
  --text-2xl: 1.5rem;    /* 24px */
  --text-3xl: 1.75rem;   /* 28px */
  --text-4xl: 2rem;      /* 32px */
  --text-5xl: 2.5rem;    /* 40px */

  /* Espaciado base 8px */
  --space-1: 0.5rem;     /* 8px */
  --space-2: 1rem;       /* 16px */
  --space-3: 1.5rem;     /* 24px */
  --space-4: 2rem;       /* 32px */
  --space-5: 2.5rem;     /* 40px */
  --space-6: 3rem;       /* 48px */
  --space-8: 4rem;       /* 64px */
  --space-10: 5rem;      /* 80px */
  --space-12: 6rem;      /* 96px */

  /* Layout */
  --container-max: 1200px;
  --radius: 4px;
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);
  --shadow-md: 0 4px 12px rgba(0,0,0,0.08);
  --shadow-lg: 0 8px 24px rgba(0,0,0,0.12);

  /* Transiciones */
  --transition-fast: 150ms ease;
  --transition-base: 250ms ease;
}

/* --------------------------------------------
   RESET Y BASE
   -------------------------------------------- */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
  font-size: 100%;
  -webkit-text-size-adjust: 100%;
}

body {
  font-family: var(--font-body);
  font-size: var(--text-base);
  line-height: 1.65;
  color: var(--text-primary);
  background-color: var(--white);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

img {
  max-width: 100%;
  height: auto;
  display: block;
}

a {
  color: var(--benevia-blue);
  text-decoration: none;
  transition: color var(--transition-fast), outline var(--transition-fast);
}

a:hover {
  color: var(--benevia-blue-dark);
  text-decoration: underline;
}

a:focus-visible,
button:focus-visible,
input:focus-visible,
textarea:focus-visible {
  outline: 3px solid var(--benevia-cyan);
  outline-offset: 2px;
  border-radius: var(--radius);
}

/* Saltar a contenido (accesibilidad) */
.skip-link {
  position: absolute;
  top: -40px;
  left: 0;
  background: var(--benevia-dark);
  color: var(--white);
  padding: var(--space-2);
  z-index: 10000;
  transition: top var(--transition-fast);
}

.skip-link:focus {
  top: 0;
}

/* --------------------------------------------
   TIPOGRAFÍA
   -------------------------------------------- */
h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-titles);
  font-weight: 700;
  line-height: 1.2;
  color: var(--benevia-dark);
  margin-bottom: var(--space-3);
}

h1 {
  font-size: var(--text-3xl);
  font-weight: 800;
  letter-spacing: -0.02em;
}

h2 {
  font-size: var(--text-2xl);
  font-weight: 700;
}

h3 {
  font-size: var(--text-xl);
  font-weight: 600;
}

h4 {
  font-size: var(--text-lg);
  font-weight: 600;
}

p {
  margin-bottom: var(--space-3);
}

p:last-child {
  margin-bottom: 0;
}

strong {
  font-weight: 600;
  color: var(--benevia-dark);
}

/* --------------------------------------------
   LAYOUT — CONTENEDOR
   -------------------------------------------- */
.container {
  width: 100%;
  max-width: var(--container-max);
  margin: 0 auto;
  padding-left: var(--space-3);
  padding-right: var(--space-3);
}

/* --------------------------------------------
   HEADER Y NAVEGACIÓN
   -------------------------------------------- */
.site-header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  background: var(--white);
  border-bottom: 1px solid var(--light-gray);
  z-index: 1000;
  box-shadow: var(--shadow-sm);
}

.header-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 64px;
}

.logo-link {
  display: flex;
  align-items: center;
  flex-shrink: 0;
}

.logo-header {
  height: 40px;
  width: auto;
}

/* Menú hamburguesa */
.menu-toggle {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 44px;
  height: 44px;
  background: transparent;
  border: none;
  cursor: pointer;
  padding: var(--space-1);
  gap: 5px;
}

.hamburger-line {
  display: block;
  width: 24px;
  height: 2px;
  background: var(--benevia-dark);
  transition: transform var(--transition-base), opacity var(--transition-base);
  border-radius: 1px;
}

.menu-toggle[aria-expanded="true"] .hamburger-line:nth-child(1) {
  transform: translateY(7px) rotate(45deg);
}

.menu-toggle[aria-expanded="true"] .hamburger-line:nth-child(2) {
  opacity: 0;
}

.menu-toggle[aria-expanded="true"] .hamburger-line:nth-child(3) {
  transform: translateY(-7px) rotate(-45deg);
}

/* Navegación */
.main-nav {
  position: absolute;
  top: 64px;
  left: 0;
  width: 100%;
  background: var(--white);
  border-bottom: 1px solid var(--light-gray);
  max-height: 0;
  overflow: hidden;
  transition: max-height var(--transition-base);
}

.main-nav.open {
  max-height: 80vh;
  overflow-y: auto;
  box-shadow: var(--shadow-md);
}

.nav-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  padding: var(--space-2) var(--space-3);
  gap: var(--space-1);
}

.nav-list a {
  display: block;
  padding: var(--space-2) var(--space-1);
  color: var(--text-primary);
  font-weight: 500;
  font-size: var(--text-base);
  border-radius: var(--radius);
  text-decoration: none;
}

.nav-list a:hover {
  background: var(--cream);
  color: var(--benevia-blue-dark);
  text-decoration: none;
}

.nav-cta {
  background: var(--benevia-blue) !important;
  color: var(--white) !important;
  text-align: center;
  margin-top: var(--space-1);
}

.nav-cta:hover {
  background: var(--benevia-blue-dark) !important;
}

/* --------------------------------------------
   BOTONES
   -------------------------------------------- */
.btn {
  display: inline-block;
  font-family: var(--font-titles);
  font-weight: 600;
  font-size: var(--text-base);
  padding: var(--space-2) var(--space-4);
  border-radius: var(--radius);
  border: 2px solid transparent;
  cursor: pointer;
  text-align: center;
  text-decoration: none;
  transition: background var(--transition-fast), transform var(--transition-fast), box-shadow var(--transition-fast);
}

.btn:active {
  transform: translateY(1px);
}

.btn-primary {
  background: var(--benevia-blue);
  color: var(--white);
  border-color: var(--benevia-blue);
}

.btn-primary:hover {
  background: var(--benevia-blue-dark);
  border-color: var(--benevia-blue-dark);
  color: var(--white);
  text-decoration: none;
  box-shadow: var(--shadow-md);
}

.btn-full {
  width: 100%;
}

/* --------------------------------------------
   SECCIONES GENERALES
   -------------------------------------------- */
.section {
  padding: var(--space-8) 0;
}

.section-title {
  font-size: var(--text-2xl);
  margin-bottom: var(--space-4);
  color: var(--benevia-dark);
}

.section-title.large {
  font-size: var(--text-3xl);
}

.section-title.light {
  color: var(--white);
}

.section-intro {
  font-size: var(--text-lg);
  max-width: 700px;
  margin-bottom: var(--space-5);
}

.section-intro.light {
  color: rgba(255,255,255,0.9);
}

.section-tag {
  display: inline-block;
  font-family: var(--font-titles);
  font-size: var(--text-xs);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--benevia-orange);
  margin-bottom: var(--space-2);
}

.section-header {
  margin-bottom: var(--space-5);
  max-width: 800px;
}

.section-cta {
  margin-top: var(--space-6);
  text-align: center;
}

.section-tagline {
  text-align: center;
  font-family: var(--font-titles);
  font-weight: 600;
  font-size: var(--text-lg);
  color: var(--benevia-dark);
  margin-top: var(--space-6);
  padding-top: var(--space-4);
  border-top: 2px solid var(--light-gray);
}

/* Fondos de sección */
.section-light {
  background: var(--white);
}

.section-dark {
  background: var(--benevia-dark);
  color: var(--white);
}

.section-dark h2,
.section-dark h3,
.section-dark strong {
  color: var(--white);
}

.section-accent {
  background: var(--cream);
}

.section-owner {
  background: var(--white);
  border-top: 4px solid var(--benevia-blue);
}

.section-family {
  background: var(--cream);
  border-top: 4px solid var(--benevia-orange);
}

.section-operator {
  background: var(--white);
  border-top: 4px solid var(--benevia-green);
}

.section-corporate {
  background: var(--cream);
  border-top: 4px solid var(--benevia-gray);
}

.section-contact {
  background: var(--benevia-dark);
  color: var(--white);
}

.section-contact h2,
.section-contact h3,
.section-contact strong {
  color: var(--white);
}

/* --------------------------------------------
   HERO
   -------------------------------------------- */
.hero {
  padding: calc(64px + var(--space-10)) 0 var(--space-10);
  background: linear-gradient(135deg, var(--cream) 0%, var(--white) 100%);
}

.hero-content {
  max-width: 800px;
}

.hero h1 {
  font-size: var(--text-3xl);
  margin-bottom: var(--space-4);
  color: var(--benevia-dark);
}

.hero-subhead {
  font-size: var(--text-lg);
  color: var(--text-secondary);
  margin-bottom: var(--space-5);
  line-height: 1.7;
}

/* --------------------------------------------
   CARDS / TARJETAS
   -------------------------------------------- */
.cards-grid {
  display: grid;
  gap: var(--space-4);
}

.card {
  background: var(--white);
  border: 1px solid var(--light-gray);
  border-radius: var(--radius);
  padding: var(--space-4);
  box-shadow: var(--shadow-sm);
  transition: box-shadow var(--transition-base), transform var(--transition-base);
}

.card:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-2px);
}

.card h3 {
  font-size: var(--text-lg);
  margin-bottom: var(--space-2);
  color: var(--benevia-blue-dark);
}

.card p {
  font-size: var(--text-base);
  color: var(--text-secondary);
  margin-bottom: var(--space-3);
}

.card-link {
  font-family: var(--font-titles);
  font-weight: 600;
  font-size: var(--text-sm);
  color: var(--benevia-blue);
  text-decoration: none;
}

.card-link:hover {
  color: var(--benevia-blue-dark);
  text-decoration: underline;
}

/* --------------------------------------------
   BULLETS Y LISTAS
   -------------------------------------------- */
.bullets-grid {
  display: grid;
  gap: var(--space-4);
  margin-bottom: var(--space-6);
}

.bullet-item {
  display: flex;
  gap: var(--space-3);
  align-items: flex-start;
}

.bullet-number {
  font-family: var(--font-titles);
  font-size: var(--text-2xl);
  font-weight: 800;
  color: var(--benevia-orange);
  line-height: 1;
  flex-shrink: 0;
  min-width: 2.5rem;
}

.bullet-item p {
  margin: 0;
  color: rgba(255,255,255,0.9);
}

.bullet-list {
  list-style: none;
  padding: 0;
  margin-bottom: var(--space-4);
}

.bullet-list li {
  position: relative;
  padding-left: var(--space-4);
  margin-bottom: var(--space-3);
  line-height: 1.7;
}

.bullet-list li::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0.6em;
  width: 8px;
  height: 8px;
  background: var(--benevia-orange);
  border-radius: 50%;
}

.bullet-list.featured li::before {
  background: var(--benevia-blue);
}

/* --------------------------------------------
   HIGHLIGHT BOXES
   -------------------------------------------- */
.highlight-box {
  background: var(--benevia-blue-dark);
  border-left: 4px solid var(--benevia-orange);
  padding: var(--space-4);
  border-radius: var(--radius);
  margin: var(--space-5) 0;
}

.highlight-box.small {
  padding: var(--space-3);
  margin: var(--space-4) 0;
}

.highlight-text {
  font-family: var(--font-titles);
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--white);
  margin: 0;
  line-height: 1.5;
}

.highlight-text strong {
  color: var(--benevia-gold);
}

/* --------------------------------------------
   CONTENT BLOCKS (secciones largas)
   -------------------------------------------- */
.content-block {
  margin-bottom: var(--space-6);
  max-width: 800px;
}

.content-block h3 {
  font-size: var(--text-xl);
  color: var(--benevia-dark);
  margin-bottom: var(--space-3);
  padding-bottom: var(--space-1);
  border-bottom: 2px solid var(--light-gray);
}

.block-alt {
  background: var(--cream);
  padding: var(--space-4);
  border-radius: var(--radius);
  max-width: 800px;
}

.pivot-text {
  font-family: var(--font-titles);
  font-weight: 600;
  font-size: var(--text-lg);
  color: var(--benevia-blue-dark);
  margin-top: var(--space-4);
  padding-left: var(--space-3);
  border-left: 3px solid var(--benevia-orange);
}

/* --------------------------------------------
   STEPS LIST (numerada)
   -------------------------------------------- */
.steps-list {
  list-style: none;
  counter-reset: step;
  padding: 0;
}

.steps-list li {
  position: relative;
  padding-left: var(--space-5);
  margin-bottom: var(--space-4);
  counter-increment: step;
}

.steps-list li::before {
  content: counter(step);
  position: absolute;
  left: 0;
  top: 0;
  font-family: var(--font-titles);
  font-size: var(--text-xl);
  font-weight: 800;
  color: var(--benevia-orange);
  line-height: 1;
}

.steps-list strong {
  display: block;
  font-size: var(--text-lg);
  margin-bottom: var(--space-1);
}

.steps-list p {
  color: var(--text-secondary);
  margin: 0;
}

/* --------------------------------------------
   DIAGNÓSTICO
   -------------------------------------------- */
.diagnostico-content {
  max-width: 800px;
}

.diagnostico-content p {
  font-size: var(--text-lg);
  color: var(--text-secondary);
  margin-bottom: var(--space-3);
}

/* --------------------------------------------
   PRESENCIA
   -------------------------------------------- */
.presencia-grid {
  display: grid;
  gap: var(--space-5);
}

.presencia-list {
  font-family: var(--font-titles);
  font-weight: 600;
  font-size: var(--text-lg);
  color: var(--benevia-blue-dark);
  line-height: 1.8;
}

.confidencialidad-box {
  background: var(--cream);
  border-left: 3px solid var(--benevia-green);
  padding: var(--space-4);
  border-radius: var(--radius);
}

.confidencialidad-box h3 {
  font-size: var(--text-base);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--benevia-green-dark);
  margin-bottom: var(--space-2);
}

.confidencialidad-box p {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  margin: 0;
}

/* --------------------------------------------
   CONTACTO / FORMULARIO
   -------------------------------------------- */
.contact-grid {
  display: grid;
  gap: var(--space-6);
}

.contact-info p {
  color: rgba(255,255,255,0.85);
  font-size: var(--text-lg);
}

.contact-note {
  font-size: var(--text-sm) !important;
  color: var(--mid-gray) !important;
  margin-top: var(--space-3);
}

.contact-form {
  background: var(--white);
  padding: var(--space-4);
  border-radius: var(--radius);
  box-shadow: var(--shadow-lg);
}

.form-group {
  margin-bottom: var(--space-3);
}

.form-group label {
  display: block;
  font-family: var(--font-titles);
  font-weight: 600;
  font-size: var(--text-sm);
  color: var(--benevia-dark);
  margin-bottom: var(--space-1);
}

.form-group label span {
  color: var(--benevia-orange-red);
}

.form-group input,
.form-group textarea {
  width: 100%;
  padding: var(--space-2);
  border: 1px solid var(--light-gray);
  border-radius: var(--radius);
  font-family: var(--font-body);
  font-size: var(--text-base);
  color: var(--text-primary);
  background: var(--white);
  transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
}

.form-group input:focus,
.form-group textarea:focus {
  border-color: var(--benevia-blue);
  box-shadow: 0 0 0 3px rgba(0,112,184,0.15);
  outline: none;
}

.form-group input::placeholder,
.form-group textarea::placeholder {
  color: var(--mid-gray);
}

.form-privacy {
  font-size: var(--text-xs);
  color: var(--mid-gray);
  text-align: center;
  margin-top: var(--space-3);
  margin-bottom: 0;
}

/* --------------------------------------------
   FOOTER
   -------------------------------------------- */
.site-footer {
  background: var(--benevia-dark);
  color: var(--white);
  padding: var(--space-8) 0 var(--space-4);
}

.footer-grid {
  display: grid;
  gap: var(--space-5);
}

.footer-brand {
  text-align: center;
}

.logo-footer {
  margin: 0 auto var(--space-2);
  width: 100px;
  height: auto;
}

.footer-slogan {
  font-family: var(--font-titles);
  font-weight: 600;
  font-size: var(--text-sm);
  color: var(--benevia-gold);
  letter-spacing: 0.05em;
}

.footer-nav h4,
.footer-segments h4,
.footer-legal h4 {
  font-size: var(--text-sm);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--mid-gray);
  margin-bottom: var(--space-2);
}

.footer-nav ul,
.footer-segments ul {
  list-style: none;
  padding: 0;
}

.footer-nav li,
.footer-segments li {
  margin-bottom: var(--space-1);
}

.footer-nav a,
.footer-segments a {
  color: rgba(255,255,255,0.8);
  font-size: var(--text-sm);
  text-decoration: none;
}

.footer-nav a:hover,
.footer-segments a:hover {
  color: var(--white);
  text-decoration: underline;
}

.footer-legal p {
  font-size: var(--text-xs);
  color: var(--mid-gray);
  margin-bottom: var(--space-1);
}

.footer-tagline {
  font-family: var(--font-titles);
  font-weight: 600;
  color: var(--white) !important;
  margin-top: var(--space-2);
}

/* ============================================
   RESPONSIVE — TABLET (768px)
   ============================================ */
@media (min-width: 768px) {
  :root {
    --text-3xl: 2rem;      /* 32px */
    --text-4xl: 2.25rem;   /* 36px */
    --text-5xl: 2.75rem;   /* 44px */
  }

  .container {
    padding-left: var(--space-4);
    padding-right: var(--space-4);
  }

  .section {
    padding: var(--space-10) 0;
  }

  .hero {
    padding: calc(64px + var(--space-12)) 0 var(--space-12);
  }

  .hero h1 {
    font-size: var(--text-4xl);
  }

  .section-title.large {
    font-size: var(--text-4xl);
  }

  .cards-grid.three-col {
    grid-template-columns: repeat(3, 1fr);
  }

  .bullets-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .presencia-grid {
    grid-template-columns: 1fr 1fr;
    align-items: start;
  }

  .contact-grid {
    grid-template-columns: 1fr 1fr;
    align-items: start;
  }

  .footer-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .content-block {
    padding-right: var(--space-4);
  }
}

/* ============================================
   RESPONSIVE — DESKTOP (1200px)
   ============================================ */
@media (min-width: 1200px) {
  :root {
    --text-4xl: 2.5rem;    /* 40px */
    --text-5xl: 3rem;      /* 48px */
  }

  .container {
    padding-left: var(--space-5);
    padding-right: var(--space-5);
  }

  .header-container {
    height: 72px;
  }

  .logo-header {
    height: 48px;
  }

  /* Menú horizontal en desktop */
  .menu-toggle {
    display: none;
  }

  .main-nav {
    position: static;
    width: auto;
    max-height: none;
    overflow: visible;
    background: transparent;
    border: none;
    box-shadow: none;
  }

  .nav-list {
    flex-direction: row;
    align-items: center;
    padding: 0;
    gap: var(--space-1);
  }

  .nav-list a {
    padding: var(--space-1) var(--space-2);
    font-size: var(--text-sm);
    white-space: nowrap;
  }

  .nav-cta {
    margin-top: 0;
    margin-left: var(--space-2);
    padding: var(--space-1) var(--space-3) !important;
    border-radius: var(--radius);
  }

  .hero h1 {
    font-size: var(--text-5xl);
    max-width: 900px;
  }

  .hero-subhead {
    font-size: var(--text-xl);
    max-width: 700px;
  }

  .section-title.large {
    font-size: var(--text-5xl);
  }

  .footer-grid {
    grid-template-columns: 1.5fr 1fr 1fr 1.5fr;
    gap: var(--space-6);
  }

  .footer-brand {
    text-align: left;
  }

  .logo-footer {
    margin: 0 0 var(--space-2) 0;
  }
}

/* ============================================
   UTILIDADES Y ESTADOS
   ============================================ */
.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

/* Reducir movimiento para usuarios que lo prefieren */
@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }

  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* ============================================
   PÁGINA DE METODOLOGÍA — clases específicas
   (antes eran estilos inline; unificados aquí)
   ============================================ */

/* Hero de metodología — offset del header fijo */
.met-hero {
  padding: 80px 0 60px;
  background: var(--benevia-dark);
  color: var(--white);
}

.met-hero h1,
.met-hero h2,
.met-hero h3 {
  color: var(--white);
}

/* Contenido de hero centrado */
.hero-content--centered {
  text-align: center;
  max-width: 780px;
  margin: 0 auto;
}

/* Subtítulo del hero de metodología */
.hero-subtitle {
  font-size: var(--text-lg);
  margin-top: var(--space-2);
  opacity: 0.9;
  line-height: 1.7;
}

/* Secciones de contenido de metodología */
.content-section {
  padding: var(--space-8) 0;
}

.content-section--white {
  background: var(--white);
}

.content-section--gray {
  background: #f8f8f8;
}

/* Grid de bloques de contenido */
.content-grid {
  display: grid;
  gap: var(--space-5);
}

.content-grid.single-col {
  max-width: 800px;
}

.content-grid--centered {
  text-align: center;
}

/* Párrafos seguidos dentro de content-block */
.content-block p + p {
  margin-top: var(--space-2);
}

/* Texto blanco dentro de highlight-box (sin clase adicional) */
.highlight-box p {
  color: var(--white);
}

/* Texto introductorio de CTA centrado */
.cta-text {
  max-width: 640px;
  margin: var(--space-2) auto 0;
}

/* Contenedor del botón CTA */
.cta-actions {
  margin-top: var(--space-4);
}

@media (min-width: 768px) {
  .content-section {
    padding: var(--space-10) 0;
  }

  .content-grid:not(.single-col) {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1200px) {
  .met-hero {
    padding: 100px 0 80px;
  }
}
